@import '../../style/config.styl';

.card
	margin ($standardGap / 2)
	padding $standardGap
	display inline-block
	border 1px solid #ccc
	li 
		margin-bottom .2em
	&:hover 
		cursor pointer
		border-color $colorPrimary
		outline 1px solid $colorPrimary